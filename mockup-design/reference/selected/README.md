# Selected reference pages

These seven pages are frozen from commit `a7001ac`, before the detailed-background
and scroll-motion experiments. Open `index.html` in this directory to review them.
The copied template, data, artwork, styles, theme script, and product screenshot
are local to this folder, so future edits to the active mockups do not alter
these fallback designs. The earlier original 04/10 references remain in the
parent `reference/` directory.
