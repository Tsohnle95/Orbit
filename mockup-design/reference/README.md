# Frozen website references

These files are a byte-for-byte snapshot of the selected Orbit mockup pages at
commit `cdea7ab`. Keep this folder unchanged; build new visual explorations in
the parent folder.

- Direction 04: `variation.html?direction=04`
- Direction 10: `variation.html?direction=10`

The snapshot includes its own chooser, styles, scripts, and product screenshots,
so future edits to the working mockups will not change these fallback pages.
